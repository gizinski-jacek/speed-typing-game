import { InputCheckPipe } from './input-check.pipe';

describe('InputCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new InputCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
